import React from 'react';
import '../styles/fakechat.css';

const FakeChat = () => (
    <div className="fakeChat">
        <input type="text"
            placeholder="Chat doesn't work due to being hosted on Github Pages, but you can always checkout the Contacts tab to contact me!"></input>
    </div>
)

export default FakeChat;